# Changelog

本插件版本变更记录。版本号遵循语义化版本。

## [0.6.2] - 2026-08-07

### 修复

- 损坏备份按 mtime 保留最近 5 份（原按文件名串序，time_ns 变长整数时可能删错）。
- 回复文本提取统一类型防御（结构化 content 不再误入记录）；custom_cliches 超长词构造期过滤并告警；损坏加载补 warning。
- 热路径微调：扫描只认 runtime marker、stable 迁移清理按会话一次、avoid_openers 统一小写前置。

### 测试

- 85→109 例全绿（四路独立复审收口）：包导入冒烟 env 隔离 + 模块归属断言，锁/备份排序/retention/status_text/group id 等盲区补齐；三项红灯实测真实捕获。

## [0.6.1] - 2026-08-07

### 修复

- 契约显式化：宿主版本上界 `<5`、损坏状态条目级容错统一、超长词口径统一（>20 字不生效）。
- 热路径收敛：懒扫描、双重 strip、replace 多块自愈、record/写盘同一把锁、disabled 快照、role 过滤。
- README/schema 与行为逐条对齐（提示长度、优先级措辞去断言化）。

### 测试

- 71→85 例全绿；P0-3/P1-6 两项红灯实测真实捕获。

## [0.6.0] - 2026-08-06

### 变更

- 版本下限提升至 AstrBot `>=4.23,<5`；稳定规则迁入 system_prompt（幂等、不入历史、旧块自动清理）。
- 稳定规则 990→五类短约束（≤550 字符）；默认检测收紧为高置信度信号；重复 opener 阈值 2→3。
- 持久化出事件循环（asyncio.to_thread）；删除从未被读取的假 version 字段。

### 测试

- 63→71 例全绿（真实宿主 4.23.3）。

## [0.5.6] - 2026-08-06

### 修复

- 4.23+ 历史中残留的旧动态块随状态更新同步移除（失效的旧约束不再留在上下文）；str 形态历史不做不安全切割。

### 变更

- `quality_rules.remove_marker_in_contexts` 新增（list 形态安全删除，无额外全量扫描）；测试 56→63 例。

## [0.5.5] - 2026-08-06

### 变更

- 移除 voice_match 声音校准整链路；`/humanq preview` 命令移除。
- 移除生产零调用的死 API；config_get 删死分支；store 属性改名消除同名歧义（持久化键不变）。
- 测试 75→56 例全绿（真实宿主契约统一归 `tests/test_real_env.py`）。

## [0.5.4] - 2026-08-06

### 变更

- 内部重构，无用户侧行为变化（测试 61→67 例）：宿主桩移出 main.py 至 `tests/_fakes.py`、每轮 marker 扫描收敛为单次、`_clip`/`_clip_hint` 合并、常量收敛。

## [0.5.3] - 2026-08-05

### 变更

- 文档结构：版本历史从 README 拆分至本文件；README 只保留定位、安装与用法。
- 更新说明条目极简化，每个版本只记录关键变更。

## [0.5.2] - 2026-08-05

### 修复

- 4.23+ 注入块被持久化并逐轮累积：历史已有 marker 不再重复注入；动态提示每轮原位替换。
- 状态文件缺键误判损坏导致清空；损坏备份限 5 份；contexts 扫描限定 user 消息。

### 变更

- TextPart 探测三态缓存；`/humanq status` 限 ADMIN；metadata 补 license、版本去 `v` 前缀；测试 61 例。

## [0.5.1] - 2026-08-05

### 修复

- 移除无法被 provider 消费的 `_FallbackTextPart`（失败回退 system 注入）；voice 样本清洗；词表微调。

## [0.5.0] - 2026-08-05

### 变更

- `tests/` 测试套件（20 例）；`custom_cliches` 配置项；metadata/作者/死代码清理。

## [0.4.0] - 2026-08-05

### 新增

- 声音校准 `voice_match`：按会话风格注入句长/语气词/表情/开头词提示（默认关，样本 ≥5 生效）。

## [0.3.0] - 2026-08-05

### 变更

- 规则升级 v2：铁律/词汇/结构/沟通/风格/自查 六层；套路词 39 个；5 种结构级正则检测。

## [0.2.0] - 2026-07-14

### 变更

- 默认 `cache_friendly` 注入模式；修复 `extra_user_content_parts is None` 时静默失败。
