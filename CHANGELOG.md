# Changelog

本插件版本变更记录。版本号遵循语义化版本。

## [3.2.0] - 2026-09-04

- **日常对话与问答实用性重塑**：对齐上游 natural-talk 最新 Agent Skill 单一事实源体系，规则升级至 Rules v11。
- **核心去 AI 腔准绳强化**：
  - 新增 C3 拒绝复述题目开场（严禁用“关于你提到的……”把用户问题重念一遍，首句直接给答案）；
  - 新增 C1 视觉不过度结构化（短问答保持自然段落呼吸感，严禁无端滥用列表与粗体）；
  - 强化 B1 彻底封杀翻案腔（“严禁先否定再肯定，只留肯定”）；
  - 严打泛滥破折号（严禁使用“——”制造刻意揭晓或后置补充，除代码外改用常规标点 [B5]）；
  - 新增 D6 模糊假归因治理（严禁无出处的“有研究表明/业内普遍认为”）；
  - 完善反清单正向白名单保护（操作步骤词“首先/其次”、设问引导、机制比喻与工程名词一律保留）。
- **信号检测与动态提示升级**：
  - 新增首部复述开场（`关于你提到的`、`关于您提到的`）与模糊假归因（`有研究表明`、`业内普遍认为`）高置信度检测；
  - 扩充问答汇报式冒号提示语检测（`总结如下：`、`建议如下：`等）；
  - 扩充日常口头禅前缀（`确实`、`当然`、`对的`、`没错`、`哈哈`）；
  - 动态提示增加聚合信号友好转义（如“破折号”转义为“别用破折号（——），改用常规标点”），大幅提升中小模型执行力。
- **同步校验脚本重塑**：`check_upstream_sync.py` 解耦已废弃的模板路径，直接校验上游 `SKILL.md` 与参考规范。
- 冻结评测集扩充至 207 条，维持 0 误报、0 漏报门禁。

## [3.1.0] - 2026-09-01

- 规则升级至 v10：稳定规则用 natural-talk lite 原文（去掉清理模式句），插件附加只保留事实/安全、任务优先、不把约束写进回复、铁律。
- 正式写作与创作/扮演请求让位：闲聊「写个通知给朋友」仍注入；「改这篇公文」与角色扮演不注入对话层。
- 检测：围栏同时认 `~~~`；URL 等长空格屏蔽；B6 同级连续编号小标题 ≥3 才报，普通标题打断。
- 评测门禁覆盖每个内置命中名；动态提示预算不足时优先装入可靠性信号。
- 文档与 `/humanq reset` 改为只清提醒记录；历史里的 v1–v8 规则正文仍按普通文本保留。

## [3.0.0] - 2026-09-01

- 检测器对齐上游 natural-talk：新增 D1 谄媚整句、D3 免责变体、D4 收尾扩充、D5 元话语空预告、B1 翻案变体（与其说/看似实则）、B4 关键在于冒号、C6 空泛气氛总结；评测集扩充，保持 0 误报 / 0 漏报。
- 新增危害排序：动态提示预算装不下时，可靠性损害信号（谄媚/免责）优先装入。
- 规则升级至 v9（RULES_VERSION 8→9）。
- 退役 v1–v8 旧规则块剥离签名表与旧版截断提示兼容代码（ARCHITECTURE.md D7 终态）。历史消息里的旧规则正文按普通文本保留，并继续注入当前规则；`/humanq reset` 与 14 天保留期只清理 `runtime_state.json` 的提醒记录，不删除人设或聊天历史里的旧规则字。
- `RuntimeStateStore.record_response` 的检测参数改为必传，删除内部重复检测路径。
- README 重构（快速开始上移、词表收敛为概览）；CHANGELOG 极简化（历史版本折叠，完整历史见 git tag v2.4.0）。
- 测试重组：test_core_flow 拆分为 config / flow / stats / text-extraction 四个文件。

## [2.x]（历史）

- 2.4.0 规则 v8，评测集 143 例，跨平台工具链修复
- 2.3.0 debounce 写盘，/humanq status 真实原因，冻结 detector 评测门禁
- 2.2.0 规则 v7，constants.py 阈值单源，delta 统计
- 2.1.0 修复：旧规则块不再阻断注入；发布包缺失模块修复
- 2.0.0 /humanq stats 命令，状态文件紧凑格式（体积 -60%）

## [1.x 及更早]

首个稳定版 1.0.0（2026-08-12）。0.x 完整历史见 git log / tag v2.4.0 的 CHANGELOG。
