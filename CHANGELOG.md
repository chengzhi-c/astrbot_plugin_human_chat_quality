# Changelog

本插件版本变更记录。版本号遵循语义化版本。

## [0.5.3] - 2026-08-05

### 变更

- 文档结构：版本历史从 README 拆分至本文件；README 只保留定位、安装与用法。
- 更新说明条目极简化，每个版本只记录关键变更。

## [0.5.2] - 2026-08-05

### 修复

- AstrBot `>=4.23` 注入块被持久化进会话历史并逐轮累积：`request_has_marker` 增加 contexts 扫描，历史已有 marker 不再重复注入。
- 动态提示（runtime/voice）在 4.23+ 冻结：改为每轮原位替换历史旧块，始终最新且不累积。
- 状态文件缺 `disabled_sessions` 键被误判损坏导致状态清空；损坏备份限 5 份。
- contexts 扫描限定 user 消息，模型复述/手打 marker 不再误停注入。

### 变更

- TextPart 探测三态缓存；`@register` 作者修正；`/humanq status` 限 ADMIN；`.gitignore` 忽略 `data/`；metadata 补 license、版本去 `v` 前缀；测试 61 例。

## [0.5.1] - 2026-08-05

### 修复

- 移除 `_FallbackTextPart`（无法被 provider 消费，失败回退 system 注入）。
- voice 样本清洗引用/@ 前缀；opener 与避用词取差集；截断边界；空 origin 隔离。
- 词表移除"让我们"；"然而"改次数阈值；"根据我的知识""我的能力范围"补入。

### 变更

- 稳定规则新增"不知道就直说"正向引导；测试 45 例。

## [0.5.0] - 2026-08-05

### 修复

- metadata 版本/作者同步；`_save` 全路径异常兜底；hook 顶层防御；import 收窄；死代码清理。

### 新增

- `tests/` 测试套件（20 例）；`custom_cliches` 配置项。

## [0.4.0] - 2026-08-05

### 新增

- 声音校准 `voice_match`：按会话风格注入句长/语气词/表情/开头词提示（默认关，样本 ≥5 生效）。

## [0.3.0] - 2026-08-05

### 变更

- 规则升级 v2：铁律/词汇/结构/沟通/风格/自查 六层；套路词 39 个；5 种结构级正则检测。

## [0.2.0] - 2026-07-14

### 变更

- 默认 `cache_friendly` 注入模式；修复 `extra_user_content_parts is None` 时静默失败。
