# Changelog

本插件版本变更记录。版本号遵循语义化版本。

## [2.0.0] - 2026-08-14

### 优化

- **类型安全**: 新增 `protocols.py` 定义核心接口 Protocol，替换 16 处 `Any` 类型，提升 IDE 支持和重构安全性。
- **代码质量**: 信号检测逻辑分层重构至 `signal_detectors.py`，圈复杂度降低 67%（12 → 4），易于维护和扩展。
- **存储优化**: 状态文件采用紧凑格式（字段名压缩、逗号分隔、整数时间戳），体积减少 60%，向后兼容旧格式自动迁移。
- **可观测性**: 新增运行时统计（注入次数、信号命中、清理计数），`/humanq stats` 命令可查看插件效果量化数据。

### 技术细节

- 模块化重组：核心逻辑分离为 6 个模块（protocols / signal_detectors / quality_rules / runtime_state / core / main）
- 状态格式 v2：`{"a": [...], "r": "x,y,z", "t": 123456}` 替代冗长字段名
- 所有 115 个测试通过，零破坏性变更

## [1.3.0] - 2026-08-14

- 修复动态提醒写入方式，避免请求组装失败；历史里的旧提示仍会换成最新一条。
- 固定规则升到 v6：用户直接问身份、能力或知识截止时间时，如实短答。已发布的 v5 规则升级时可替换。
- 检测增加 `I hope this helps`、`Great question`；`/humanq status` 显示下一轮避用项。

## [1.2.0] - 2026-08-14

- 同步上游 natural-talk：固定规则补充"不适用范围"行；运行时检测对齐上游计数口径——破折号按 em/en dash 计次、新增感叹号（≤3 次）、路标词/破折号/感叹号阈值按篇幅折算（300 字基准，更长按比例放宽）。
- 固定规则 marker v4→v5：v4 已发布块签名入剥离表，存量用户升级时旧块自动移除，不阻断注入。

## [1.1.1] - 2026-08-12

- 修复：严格识别并迁移本插件拥有的固定/动态注入块，兼容 LF、CRLF、历史完整块及 1.1.0 截断动态块；用户正文中的 marker、相似前缀和未知格式保持不变。
- 修复：所有关闭路径先清理可识别的历史注入再返回；关闭动态提醒时仍记录轻量状态，重新开启后保持连续。
- 修复：状态写盘失败可见、可重试，命令区分内存生效与落盘失败，退出时执行最终保存；无时间戳旧状态按文件 mtime 正确过期。
- 重构：宿主无关核心集中到 `core.py`；运行时提示只装入完整短语，配置范围按真实容量收敛为 80-157。
- 测试：严格 runner 拒绝 skip；core、最低宿主发布和最新 4.x 宿主分层验证，测试可在任意下载目录名下运行。
- 发布：版本与运行时清单不一致直接失败；zip 顶层名来自 metadata，且不包含测试、脚本、缓存或运行时数据。
- 合规：发布包保留 natural-talk 上游版权、来源与完整 MIT 许可告知。

## [1.1.0] - 2026-08-12

- 重构：群号提取收敛为两级（平台接口 → origin 解析），移除冗余的属性名探测层；会话身份函数迁入 runtime_state，无宿主可测。
- 重构：版本解析提取为纯函数；规则 marker 版本常量化（`RULES_VERSION`），旧版块剥离清单由推导生成（含无版本形态）。
- 修复：旧规则块整块剥离——旧块内部含空行分段，此前只剥 marker 段落会残留孤儿分段。
- 新增：发布脚本 `scripts/build_release.py`（测试/门禁预检、清单校验、版本一致性、打包）；发布 zip 现含 `tests/` 与 `ruff.toml`。
- 新增：GitHub Actions CI（unittest + compileall + ruff）。
- 测试：45 → 83 用例，新增禁用匹配全形态、窗口边界、reset/过期清理/备份轮转、写盘异常隔离、to_thread 写盘、回复文本链式提取、插件层异常隔离等契约用例。
- 契约覆盖说明：C6 命令层权限/路由属宿主 filter 框架行为（插件层不测）；C4 字符串形态旧块残留为设计内行为（不做不安全切割），由注释声明。
- 其他：`clip_text` 移除不可达分支；E402 豁免集中到 ruff.toml 并注明原因。

## [1.0.0] - 2026-08-12

- 首个正式版：自 0.10.0 起无功能变更，标记为稳定版本（语义化版本 1.x 约定：公开契约稳定）。

## [0.10.0] - 2026-08-12

- 稳定规则 v4：随 natural-talk 2.1.0 同步升级——表达层条目弹性化（破折号全文 ≤2 次、开场白 ≤1 句、路标词全文 ≤2 次、预告动作 ≤1 次等），原则层（诚实/不编造/不评判人）保持绝对。marker 升到 v4；v1/v2 已发布块自动剥离。无法核验的 v3 块按未知块保留。

## [0.9.0] - 2026-08-12

- 稳定规则曾尝试升级到 v3 marker，并逐字引用 natural-talk v2.0.0（MIT）"作为 System Prompt"章节原文 + 插件附加安全条款；完整 v3 正文未形成可核验发布物，现行实现将无法核验的 v3 块按未知块保留，不猜测剥离。v1/v2 已发布块自动剥离（system_prompt 按段落剥离 + 历史按 legacy marker 清扫）。
- 运行时检测扩充（延续"只抓高置信度信号"契约，信号来源 natural-talk）：AI 自我暴露短语（作为AI/根据我的训练/截至我的知识更新）任意位置命中；谄媚/预告式开场（好问题/让我来/感谢你的提问）仅回复首部命中；收尾腔总结（综上所述/由此可见）仅结尾命中；路标词（事实上/实际上/换句话说/本质上/归根结底/与此同时）同一回复 ≥3 次报堆砌。
- 测试套件随仓库发布（tests/，覆盖规则注入、legacy 剥离、未知 v3 块保留、检测信号与误报控制、Core 全流程）。

## [0.7.1] - 2026-08-07

- 三态返回收敛为 `MarkerResult` 类型常量；群号提取键序与 opener 截断长度收敛为单一常量源。

## [0.7.0] - 2026-08-07

- contexts 单趟 replace/remove；版本唯一源 `metadata.yaml`；`recent_reply_window` 下限改为 3。

## [0.6.2] - 2026-08-07

- 损坏备份按 mtime 保留 5 份；回复文本提取类型防御；超长自定义词构造期过滤。

## [0.6.1] - 2026-08-07

- 宿主版本上界 `<5`；状态条目级容错；record 与写盘共用同一把锁。

## [0.6.0] - 2026-08-06

- 稳定规则迁入 system_prompt（幂等、不入历史）；默认检测收紧为高置信度信号；持久化出事件循环。

## [0.5.6] - 2026-08-06

- 历史残留旧动态块随状态更新同步移除。

## [0.5.5] - 2026-08-06

- 移除 voice_match 声音校准链路与 `/humanq preview`。

## [0.5.4] - 2026-08-06

- 内部重构，无用户侧行为变化。

## [0.5.3] - 2026-08-05

- 版本历史从 README 拆分至本文件；条目极简化。

## [0.5.2] - 2026-08-05

- 修复注入块持久化累积与状态文件缺键误判损坏。

## [0.5.1] - 2026-08-05

- 移除无法被 provider 消费的 `_FallbackTextPart`。

## [0.5.0] - 2026-08-05

- 首个测试套件（20 例）；新增 `custom_cliches` 配置项。

## [0.4.0] - 2026-08-05

- 新增声音校准 `voice_match`（默认关）。

## [0.3.0] - 2026-08-05

- 规则升级 v2：六层约束；套路词 39 个；5 种结构级正则检测。

## [0.2.0] - 2026-07-14

- 默认 `cache_friendly` 注入模式；修复 `extra_user_content_parts` 为空时的静默失败。
